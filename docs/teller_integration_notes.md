# Teller Integration Notes

## Authentication
- Create a route so I can get my enrollment token (token used for specified bank)

## cURL Example

curl -i --cert certificate.pem --key private_key.pem  https://api.teller.io/accounts -u <token> 
